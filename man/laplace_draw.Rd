% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace.R
\name{laplace_draw}
\alias{laplace_draw}
\title{One draw from the Laplace approximation}
\usage{
laplace_draw(
  Phi,
  y,
  tau2,
  family = "gaussian",
  sigma = NULL,
  offset = 0,
  max_iter = 100L,
  tol = 1e-10
)
}
\arguments{
\item{Phi}{Frozen feature matrix, \code{n} x \code{m}.}

\item{y}{Outcome of length \code{n}. On the natural scale of the family: a
residual for \code{"gaussian"}, zeros and ones for \code{"binomial"}, non-negative
integers for \code{"poisson"}.}

\item{tau2}{Prior variance of each weight. The prior is \verb{w ~ N(0, tau2 I)}.}

\item{family}{One of \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, each with its
canonical link.}

\item{sigma}{Noise standard deviation, required for \code{"gaussian"} and refused
for the others, which have no free scale parameter.}

\item{offset}{Known part of the linear predictor, length \code{n} or a single
number. The host's contribution, as in \code{\link[=set_offset]{set_offset()}}.}

\item{max_iter}{Maximum Newton iterations.}

\item{tol}{Convergence tolerance on the largest absolute step.}
}
\value{
A numeric vector of length \code{m}: one draw of \code{w}, carrying the column
names of \code{Phi} if it has any.
}
\description{
Draws from the Gaussian that \code{\link[=laplace_moments]{laplace_moments()}} fits at the posterior mode.
Approximate by construction: see that function for what the approximation
does and does not preserve.
}
\details{
This is \strong{not} a Gibbs transition kernel and must not be used as one. A
Gaussian fitted at the mode is not the conditional distribution of the
weights, so iterating it does not leave the target invariant.
\code{\link[=gibbs_step]{gibbs_step()}} refuses \code{posterior = "laplace"} unless \code{force = TRUE}, and
that refusal is the intended behaviour rather than an obstacle.

The legitimate uses are one-shot: uncertainty on a body trained outside any
sampler, and Poisson counts, which the exact augmentation cannot reach.

\strong{For more than a few draws, do not call this in a loop.} Every call
re-runs the whole Newton optimisation to find a mode that has not moved.
Call \code{\link[=laplace_moments]{laplace_moments()}} once and sample from the \code{mean} and \code{cov} it
returns; the mode is the expensive part and it is the same for every draw.
}
\examples{
set.seed(1)
Phi <- matrix(rnorm(200 * 3), 200, 3)
y <- rpois(200, exp(as.vector(Phi \%*\% c(0.5, -0.25, 0.1))))
laplace_draw(Phi, y, tau2 = 10, family = "poisson")

}
\seealso{
\code{\link[=laplace_moments]{laplace_moments()}}, \code{\link[=conjugate_draw]{conjugate_draw()}}
}
