% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_methods.R
\name{plot.bllnn_fit}
\alias{plot.bllnn_fit}
\title{Diagnostic plots for a fitted model}
\usage{
\method{plot}{bllnn_fit}(x, which = c("trace", "density", "fitted"), ...)
}
\arguments{
\item{x}{A \code{bllnn_fit}.}

\item{which}{One or more of \code{"trace"}, \code{"density"}, \code{"fitted"}.}

\item{...}{Passed to the underlying plot calls.}
}
\value{
\code{x}, invisibly. Called for the plots.
}
\description{
Two panels per linear coefficient: a trace, which must look like noise, and
the posterior density. Then the fitted \code{f(Z)} with credible bands against
the response.
}
\details{
A trace that drifts or wanders means the chain has not mixed, and the
intervals cannot be trusted whatever their width or the effective sample
size says.
}
\examples{
sim <- sim_partial_linear(n = 120, beta = c(treat = 1.5), p_z = 5, seed = 1)
fit <- bllnn(y ~ z1 + z2 + z3 + z4 + z5, data = sim$data, linear = ~ treat,
             folds = 2, width = 5, epochs = 80, n_iter = 150, burn = 50,
             seed = 1)
plot(fit, which = "trace")

}
