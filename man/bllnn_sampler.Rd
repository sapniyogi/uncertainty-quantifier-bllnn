% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_sampler.R
\name{bllnn_sampler}
\alias{bllnn_sampler}
\alias{print.bllnn_sampler}
\title{Build a last-layer Gibbs sampler block}
\usage{
bllnn_sampler(Phi, tau2, posterior = "conjugate", features = "frozen")

\method{print}{bllnn_sampler}(x, ...)
}
\arguments{
\item{Phi}{Frozen feature matrix, \code{n} x \code{m}. Learned once on a held-out
fold and fixed; this object does not check that, but the exactness of the
draw depends on it.}

\item{tau2}{Prior variance of each weight, a single positive number. The
prior is \verb{w ~ N(0, tau2 I)}.}

\item{posterior}{Which posterior to draw from. Only \code{"conjugate"} is
implemented; the other names in \code{\link[=valid_kernels]{valid_kernels()}} are accepted so that
\code{\link[=is_valid_kernel]{is_valid_kernel()}} can report on them.}

\item{features}{Whether features stay fixed during sampling. Only
\code{"frozen"} gives a valid kernel.}

\item{x}{A \code{bllnn_sampler} object.}

\item{...}{Ignored.}
}
\value{
A \code{bllnn_sampler} object.
}
\description{
Wraps a frozen feature matrix into a reusable object that supplies one step
of a Gibbs sampler written by the caller. The contract mirrors \code{dbarts}:
hand it a residual and a noise level, get back one posterior draw of the
nonlinear function.
}
\details{
\strong{Precomputation.} Frozen features means \code{Phi} is constant for the whole
run, so everything derivable from it alone is computed once here and stored:
the \code{m} x \code{m} cross-product \verb{Phi'Phi} and the prior precision \code{I / tau2}.
\code{\link[=gibbs_step]{gibbs_step()}} recomputes none of it. \verb{Phi'r} is computed once per
\code{\link[=set_response]{set_response()}} call rather than once per step, so repeated draws against
the same residual cost only the solve.

The linear algebra is deliberately naive -- a plain \code{solve()} per step. The
point of this structure is that the fast path, factoring the cross-product
once and reusing the basis, drops in later without any change to the API.

\strong{Reference semantics.} The returned object is an environment.
\code{\link[=set_response]{set_response()}} and \code{\link[=set_sigma]{set_sigma()}} modify it in place, so a host sampler
does not reassign it each sweep.

\strong{The host owns sigma.} The noise level is supplied through
\code{\link[=set_sigma]{set_sigma()}} and never estimated here. Estimating it internally would use
the data twice and break the joint chain.
}
\examples{
set.seed(1)
Phi <- matrix(rnorm(100 * 3), 100, 3)
r <- as.vector(Phi \%*\% c(1, -2, 0.5)) + rnorm(100, sd = 0.5)

mod <- bllnn_sampler(Phi, tau2 = 10)
is_valid_kernel(mod)

set_response(mod, r)
set_sigma(mod, 0.5)
f <- gibbs_step(mod)
length(f)

}
\seealso{
\code{\link[=set_response]{set_response()}}, \code{\link[=set_sigma]{set_sigma()}}, \code{\link[=gibbs_step]{gibbs_step()}},
\code{\link[=is_valid_kernel]{is_valid_kernel()}}
}
