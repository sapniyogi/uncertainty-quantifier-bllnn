% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_crossfit.R
\name{bllnn_crossfit}
\alias{bllnn_crossfit}
\alias{print.bllnn_crossfit}
\title{Cross-fitted network bodies}
\usage{
bllnn_crossfit(x, y, linear = NULL, folds = 5, seed = NULL, ...)

\method{print}{bllnn_crossfit}(x, ...)
}
\arguments{
\item{x}{A \code{bllnn_crossfit} object.}

\item{y}{Response used to train the bodies, length \code{n}.}

\item{linear}{Optional linear-term matrix, as in \code{\link[=bllnn_warmup]{bllnn_warmup()}}. Supplying
it adds the confounding channel to every fold.}

\item{folds}{Number of folds, or an integer vector of length \code{n} assigning
each row to a fold.}

\item{seed}{Optional integer seed. Controls both the fold split and the
per-fold training; the random number stream of the caller is restored.}

\item{...}{Ignored.}
}
\value{
A \code{bllnn_crossfit} object.
}
\description{
Trains one body per fold, each on the other folds, and assembles the frozen
feature matrix so that every row's features come from a body that never saw
that row's response. This is the default for causal use.
}
\details{
\strong{Why the features are block diagonal.} Each fold gets its own network, so
the \code{k}th body's hidden unit \code{h1} is a different function from the \code{j}th
body's \code{h1}. Sharing one weight vector across them would be incoherent.
Instead each fold's rows load only on that fold's columns, and the rest of
the row is zero. The posterior then factorises across blocks, so a draw from
the assembled system is exactly a set of independent per-fold draws — the
block structure is bookkeeping, not an approximation.

The cost is \code{folds * (width + 1)} columns rather than \code{width + 1}. With a
naive solve that is \code{K^2} more work than solving the blocks separately;
exploiting the block structure is part of the same deferred fast path as the
eigendecomposition, and needs no API change.

\strong{What this object is for.} It carries the features for the rows it was
built on. It is not a predictor for new data: a new row belongs to no fold,
so there is no body that is out-of-sample for it. Prediction on fresh data
is a question for the user-facing wrapper, not for this object.
}
\examples{
sim <- sim_partial_linear(n = 150, p_z = 5, seed = 1)
cf <- bllnn_crossfit(sim$Z, sim$data$y, folds = 3, width = 8,
                     epochs = 150, seed = 1)
cf
dim(feature_matrix(cf))

}
\seealso{
\code{\link[=bllnn_warmup]{bllnn_warmup()}}, \code{\link[=bllnn_sampler]{bllnn_sampler()}}
}
