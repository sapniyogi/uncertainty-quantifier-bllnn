% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polyagamma.R
\name{rpolyagamma}
\alias{rpolyagamma}
\title{Draw from the Polya-Gamma distribution}
\usage{
rpolyagamma(n, z = 0)
}
\arguments{
\item{n}{Number of draws, or omit and pass a vector \code{z}.}

\item{z}{Tilting parameter. Recycled to length \code{n}; a vector gives one draw
per element.}
}
\value{
A numeric vector of draws.
}
\description{
Exact draws from \code{PG(1, z)}, the augmentation that makes logistic
likelihoods conditionally Gaussian.
}
\details{
\strong{What it is for.} For a binary outcome the likelihood contribution is
\code{exp(psi)^y / (1 + exp(psi))}, which is not Gaussian in \code{psi} and leaves no
closed-form posterior for the weights. Polson, Scott and Windle (2013)
showed that introducing \code{omega ~ PG(1, psi)} per observation makes the
likelihood exactly Gaussian in \code{psi} given \code{omega}. Alternating between
drawing \code{omega} given the weights and the weights given \code{omega} is then an
exact Gibbs sampler, with no approximation at either step.

\strong{Why not the series representation.} \code{PG(1, z)} can be written as an
infinite weighted sum of exponentials, and truncating that sum is a common
shortcut. It is also an approximation, with a tail left over of order
\code{1 / K} after \code{K} terms. Design rule 5 asks for exact draws where they
exist, and here one does: Devroye's alternating-series method accepts or
rejects using partial sums that bracket the density from alternating sides,
so the decision is exact after finitely many terms.

The mean is \verb{tanh(z / 2) / (2 z)}, which tends to \code{1/4} as \code{z} tends to
zero.
}
\examples{
set.seed(1)
draws <- rpolyagamma(2000, z = 0)
c(mean = mean(draws), theoretical = 0.25)

# The mean shrinks as the tilt grows
vapply(c(0, 1, 4), function(z) mean(rpolyagamma(500, z)), numeric(1))

}
\references{
Polson, N. G., Scott, J. G. and Windle, J. (2013). Bayesian inference for
logistic models using Polya-Gamma latent variables. \emph{Journal of the
American Statistical Association}, 108(504), 1339-1349.
}
