% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn.R, R/bllnn_methods.R
\name{bllnn}
\alias{bllnn}
\alias{print.bllnn_fit}
\title{Fit a partial linear model with a Bayesian last layer}
\usage{
bllnn(
  formula,
  data,
  linear = NULL,
  folds = 5,
  n_iter = 2000,
  burn = 500,
  tau2 = "auto",
  prior_beta = 100,
  sigma_shape = 2,
  sigma_rate = 1,
  keep_f = TRUE,
  seed = NULL,
  ...
)

\method{print}{bllnn_fit}(x, ...)
}
\arguments{
\item{formula}{A formula \code{y ~ z1 + z2 + ...}. The right-hand side names the
variables the nonlinear function is over. No intercept is fitted for them:
the network carries its own.}

\item{data}{A data frame.}

\item{linear}{A one-sided formula naming the linear terms, for example
\code{~ treat}. These get credible intervals. \code{NULL} fits \code{y = f(Z) + e} with
no linear part.}

\item{folds}{Number of cross-fitting folds.}

\item{n_iter}{Total Gibbs iterations, including burn-in.}

\item{burn}{Iterations discarded as burn-in.}

\item{tau2}{Prior variance of the last-layer weights; see
\code{\link[=bllnn_sampler]{bllnn_sampler()}}. \code{"auto"} reads it from the residual scale.}

\item{prior_beta}{Prior variance for the linear coefficients. The default is
deliberately diffuse: these are the parameters of interest and should be
driven by the data.}

\item{sigma_shape, sigma_rate}{Inverse gamma prior for \code{sigma^2}.}

\item{keep_f}{Store every draw of the fitted function. Needed for credible
bands from \code{\link[=plot.bllnn_fit]{plot.bllnn_fit()}}; costs \code{(n_iter - burn) * n} doubles.}

\item{seed}{Optional integer seed. The random number stream of the caller is
restored on exit.}

\item{...}{Ignored.}

\item{x}{A \code{bllnn_fit}.}
}
\value{
An object of class \code{bllnn_fit}.
}
\description{
The convenience interface: \verb{y = X beta + f(Z) + e}, where \code{formula} gives
the response and the variables \code{f} is over, and \code{linear} gives the terms
that need honest credible intervals.
}
\details{
This assembles the configuration that the package's coverage simulation
validates, in this order:
\enumerate{
\item Cross-fitted network bodies, so every row's features come from a network
that never saw that row's response (\code{\link[=bllnn_crossfit]{bllnn_crossfit()}}).
\item The confounding channel: an auxiliary body per linear term estimating
\code{E[X | Z]}, appended to the features.
\item The linear terms residualised against it, via \code{\link[=partial_out]{partial_out()}}, so the
network cannot absorb the coefficient.
\item The prior variance read from the residual scale rather than guessed.
}

Each of those steps was at some point in development the thing that was
silently wrong, and each is invisible in the output when it is. Getting all
four right unaided is not a reasonable expectation, which is the reason this
function exists.

Around them it runs a Gibbs sampler: \code{beta} from its conjugate normal
conditional, \code{f} from \code{\link[=gibbs_step]{gibbs_step()}}, and \code{sigma^2} from its inverse gamma
conditional. The block itself never sees \code{y} and never estimates \code{sigma^2}.

Over 100 simulated datasets at confounding 0.6, this configuration covered
the true coefficient 96\% of the time against a nominal 95\%, with bias
-0.020 where naive least squares carried +0.397. See
\code{inst/validation/coverage_simulation.R}.
}
\examples{
sim <- sim_partial_linear(n = 150, beta = c(treat = 1.5), p_z = 5, seed = 1)
df <- sim$data

fit <- bllnn(y ~ z1 + z2 + z3 + z4 + z5, data = df, linear = ~ treat,
             folds = 2, width = 6, epochs = 100,
             n_iter = 200, burn = 50, seed = 1)
fit
coef(fit)
confint(fit)

}
\seealso{
\code{\link[=bllnn_crossfit]{bllnn_crossfit()}}, \code{\link[=partial_out]{partial_out()}}, \code{\link[=bllnn_sampler]{bllnn_sampler()}}
}
