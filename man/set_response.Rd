% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_sampler.R
\name{set_response}
\alias{set_response}
\title{Set the residual this block should explain}
\usage{
set_response(mod, r)
}
\arguments{
\item{mod}{A \code{bllnn_sampler} object.}

\item{r}{Residual vector of length \code{n}.}
}
\value{
\code{mod}, invisibly.
}
\description{
Modifies \code{mod} in place; the caller does not reassign it. \verb{Phi'r} is
computed here rather than in \code{\link[=gibbs_step]{gibbs_step()}}, so repeated draws against one
residual do not repeat the work.
}
\examples{
Phi <- matrix(rnorm(30), 10, 3)
mod <- bllnn_sampler(Phi, tau2 = 1)
set_response(mod, rnorm(10))

}
