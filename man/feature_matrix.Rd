% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_warmup.R
\name{feature_matrix}
\alias{feature_matrix}
\title{Frozen features from a warmed-up body}
\usage{
feature_matrix(body, newdata)
}
\arguments{
\item{body}{A \code{bllnn_body} from \code{\link[=bllnn_warmup]{bllnn_warmup()}}.}

\item{newdata}{Predictor matrix or data frame with the same columns used in
training.}
}
\value{
A numeric matrix, \code{nrow(newdata)} x \code{(width + 1)}. The first column
is the intercept.
}
\description{
Applies the stored standardisation and returns the hidden activations with a
leading intercept column. This is the matrix \code{\link[=bllnn_sampler]{bllnn_sampler()}} draws a last
layer over.
}
\examples{
sim <- sim_partial_linear(n = 120, p_z = 5, seed = 1)
r <- sim$data$y - as.vector(sim$X \%*\% sim$beta)
body <- bllnn_warmup(sim$Z, r, width = 10, epochs = 200, seed = 1)
Phi <- feature_matrix(body, sim$Z)
dim(Phi)

}
