% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_crossfit.R, R/bllnn_warmup.R
\name{feature_matrix}
\alias{feature_matrix}
\alias{feature_matrix.default}
\alias{feature_matrix.bllnn_crossfit}
\alias{feature_matrix.bllnn_body}
\title{Frozen features}
\usage{
feature_matrix(object, newdata = NULL, ...)

\method{feature_matrix}{default}(object, newdata = NULL, ...)

\method{feature_matrix}{bllnn_crossfit}(object, newdata = NULL, ...)

\method{feature_matrix}{bllnn_body}(object, newdata = NULL, ...)
}
\arguments{
\item{object}{A \code{bllnn_body} from \code{\link[=bllnn_warmup]{bllnn_warmup()}}, or a \code{bllnn_crossfit}
from \code{\link[=bllnn_crossfit]{bllnn_crossfit()}}.}

\item{newdata}{Predictors to evaluate on. Required for a \code{bllnn_body};
must be \code{NULL} for a \code{bllnn_crossfit}, which carries the features for the
rows it was built on.}

\item{...}{Ignored.}
}
\value{
A numeric feature matrix.
}
\description{
Frozen features
}
\details{
For a \code{bllnn_body} this applies the stored standardisation and returns the
hidden activations with a leading intercept column, plus one confounding
channel per linear term if the body was warmed up with any.
}
\examples{
sim <- sim_partial_linear(n = 120, p_z = 5, seed = 1)
body <- bllnn_warmup(sim$Z, sim$data$y, width = 10, epochs = 150, seed = 1)
dim(feature_matrix(body, sim$Z))

}
