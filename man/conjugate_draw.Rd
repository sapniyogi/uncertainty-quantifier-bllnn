% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conjugate_draw.R
\name{conjugate_draw}
\alias{conjugate_draw}
\title{One exact draw of the last-layer weights}
\usage{
conjugate_draw(Phi, r, sigma, tau2)
}
\arguments{
\item{Phi}{Fixed feature matrix, \code{n} x \code{m}. Frozen during sampling: the draw
is exactly correct conditionally only because \code{Phi} does not depend on the
response being fitted.}

\item{r}{Residual vector of length \code{n}, the part of the response this block
is being asked to explain.}

\item{sigma}{Noise standard deviation, a single positive number. Supplied by
the caller, never estimated internally.}

\item{tau2}{Prior variance of each weight, a single positive number. The
prior is \verb{w ~ N(0, tau2 I)}.}
}
\value{
A numeric vector of length \code{m}: one draw of \code{w}. If \code{Phi} has column
names they are carried through as the names of the result.
}
\description{
Draws a single vector \code{w} from the exact conditional posterior of the last
layer of a Bayesian Last Layer network, given frozen features.
}
\details{
For frozen features \code{Phi}, residual \code{r}, known noise scale \code{sigma}, and prior
\verb{w ~ N(0, tau2 I)}, the model

\deqn{r \mid w \sim N(\Phi w, \sigma^2 I)}

has the exactly Gaussian conditional posterior

\deqn{w \mid r, \sigma^2 \sim N(V \Phi' r / \sigma^2, V), \quad
      V = (\Phi' \Phi / \sigma^2 + I / \tau^2)^{-1}.}

This is drawn in closed form. No optimisation, MCMC, or variational
approximation is involved, which is what makes the result usable as an exact
Gibbs transition kernel.

Two conventions matter and are easy to get backwards:
\code{sigma} is the noise \strong{standard deviation}, while \code{tau2} is the prior
\strong{variance}. Passing a variance as \code{sigma} will not error; it will quietly
return draws with the wrong spread.

The noise scale is an argument, never estimated here. In a Gibbs sampler the
host owns \code{sigma^2} and updates it in its own step; estimating it inside this
draw would use the data twice and break the joint chain.

Because the prior is proper, the draw is well defined even when \code{Phi} has
more columns than rows.
}
\examples{
set.seed(1)
Phi <- matrix(rnorm(100 * 4), nrow = 100, ncol = 4)
w_true <- c(1, -2, 0.5, 0)
r <- as.vector(Phi \%*\% w_true) + rnorm(100, sd = 0.5)

conjugate_draw(Phi, r, sigma = 0.5, tau2 = 10)

# Repeated draws trace out the posterior
draws <- t(replicate(1000, conjugate_draw(Phi, r, sigma = 0.5, tau2 = 10)))
colMeans(draws)

}
