% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_warmup.R
\name{bllnn_warmup}
\alias{bllnn_warmup}
\alias{print.bllnn_body}
\title{Learn a frozen network body}
\usage{
bllnn_warmup(
  x,
  y,
  width = 50,
  epochs = 2000,
  activation = c("relu", "tanh"),
  learn_rate = 0.01,
  weight_decay = 0.01,
  validation = 0.25,
  patience = 300,
  seed = NULL
)

\method{print}{bllnn_body}(x, ...)
}
\arguments{
\item{x}{A \code{bllnn_body} object.}

\item{y}{Response for the warm-up fit, length \code{n}. In partial-linear use
this is the part of the outcome the body is meant to explain.}

\item{width}{Number of hidden units.}

\item{epochs}{Maximum training epochs.}

\item{activation}{\code{"relu"} or \code{"tanh"}.}

\item{learn_rate}{Adam step size.}

\item{weight_decay}{Decoupled weight decay. \code{0} disables it. Values around
\code{0.01} help; large values such as \code{0.1} over-smooth and degrade the fit.}

\item{validation}{Fraction of \code{x} held out to select the stopping epoch.}

\item{patience}{Stop after this many epochs with no validation improvement.}

\item{seed}{Optional integer seed. The random number stream of the caller is
restored on exit.}

\item{...}{Ignored.}
}
\value{
A \code{bllnn_body} object. Pass it to \code{\link[=feature_matrix]{feature_matrix()}} to obtain the
frozen features, or straight to \code{\link[=bllnn_sampler]{bllnn_sampler()}} with \code{data}.
}
\description{
Trains a single-hidden-layer network on a seed fold and freezes it. The
hidden activations become the fixed feature matrix that
\code{\link[=bllnn_sampler]{bllnn_sampler()}} draws a last layer over.
}
\details{
\strong{Why the body is frozen.} Sampling conditions on the features. If they
were refit against the response being sampled, the conjugate draw would no
longer be exactly correct conditionally. Train once, here, on data the
sampler will not reuse; then never touch it again. For causal work the
calling code should cross-fit, training a body per fold and sampling on the
held-out one.

\strong{Training.} Full-batch Adam with decoupled weight decay on the weight
matrices but not the biases. A slice of the supplied data is held out for
validation; the parameters returned are those from the epoch with the best
validation loss, not the last epoch. That matters: without it the body
interpolates the training fold, and generalisation gets monotonically worse
with more epochs.

\strong{Standardisation.} Predictors are centred and scaled using statistics
computed here and stored on the object, so \code{\link[=feature_matrix]{feature_matrix()}} applies the
identical transformation to new data. Recomputing them per-dataset would
silently produce a different feature map for the sampling fold.
}
\examples{
sim <- sim_partial_linear(n = 200, p_z = 5, seed = 1)
r <- sim$data$y - as.vector(sim$X \%*\% sim$beta)

body <- bllnn_warmup(sim$Z, r, width = 20, epochs = 300, seed = 1)
body
dim(feature_matrix(body, sim$Z))

}
\seealso{
\code{\link[=feature_matrix]{feature_matrix()}}, \code{\link[=bllnn_sampler]{bllnn_sampler()}}
}
