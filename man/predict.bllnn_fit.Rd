% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bllnn_methods.R
\name{predict.bllnn_fit}
\alias{predict.bllnn_fit}
\title{Fitted values and predictions}
\usage{
\method{predict}{bllnn_fit}(
  object,
  newdata = NULL,
  type = c("response", "f"),
  interval = FALSE,
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \code{bllnn_fit}.}

\item{newdata}{Optional data frame. Without it, fitted values for the rows
the model was fitted to.}

\item{type}{\code{"response"} for \verb{X beta + f(Z)}, or \code{"f"} for the nonlinear
part alone.}

\item{interval}{Return posterior mean with a credible interval rather than
the mean alone. Requires \code{keep_f = TRUE} at fitting time.}

\item{level}{Interval level.}

\item{...}{Ignored.}
}
\value{
A numeric vector, or a matrix with \code{fit}, \code{lower} and \code{upper}.
}
\description{
Fitted values and predictions
}
\details{
With \code{newdata}, the nonlinear part is the average across the cross-fitting
folds of each body's features times that fold's weight draws. Cross-fitting
exists so that the conditional draw is exact for rows whose response is
being sampled; a new row has no response in the model, so there is nothing
to leak and every body is a legitimate estimate of \code{f}. Averaging them is an
ensemble, not a workaround.

\code{type = "response"} on new data needs the linear terms as well, and needs
them on the scale the model was fitted on. The model is fitted against
\code{X - E[X | Z]}, so the prediction subtracts the same quantity, averaged over
folds exactly as \code{f} is. Using the raw column instead would shift every
prediction by \code{beta * E[X | Z]}. The auxiliary bodies supply \code{E[X | Z]} for
rows they never saw, so nothing additional is estimated -- and it follows
that a fit built without \code{linear} cannot do this, and says so rather than
guessing.
}
\examples{
sim <- sim_partial_linear(n = 120, beta = c(treat = 1.5), p_z = 5, seed = 1)
fit <- bllnn(y ~ z1 + z2 + z3 + z4 + z5, data = sim$data, linear = ~ treat,
             folds = 2, width = 5, epochs = 80, n_iter = 150, burn = 50,
             seed = 1)
head(predict(fit))
head(predict(fit, type = "f", interval = TRUE))

}
